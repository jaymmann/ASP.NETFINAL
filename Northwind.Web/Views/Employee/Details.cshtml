@model Northwind.EntityModels.Employee

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card card-flush h-md-100 mb-5 mb-lg-10">
    <div class="card-header pt-5">
        <h3 class="card-title align-items-start flex-column">
            <span class="card-label fw-bold text-gray-900">Employee Details</span>
        </h3>
    </div>
    <div class="card-body pt-5 pb-0">
        @foreach (var prop in Model.GetType().GetProperties())
        {
            if (prop.Name != "Orders")
            {
                <div class="d-flex flex-stack">
                    <div class="text-gray-700 fw-semibold fs-6 me-2">@Html.DisplayName(prop.Name)</div>
                    <div class="d-flex align-items-senter">
                        <span class="text-gray-900 fw-bolder fs-6 ms-20">@prop.GetValue(Model)</span>
                    </div>
                </div>
                <div class="separator separator-dashed my-3"></div>
            }        
        }
    </div>
    <div class="card-footer text-end">
        <a asp-action="Index" class="btn btn-light btn-sm">Back to List</a>
        <a asp-action="Edit" asp-route-id="@Model?.EmployeeId" class="btn btn-primary btn-sm">Edit</a>
    </div>
</div>